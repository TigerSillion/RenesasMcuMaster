# Skill Sync Note

This repository keeps canonical skills in `.codex/skills`.
Copies are mirrored into `.agents/skills` for default Codex skill discovery compatibility.
When updating a skill, update `.codex/skills/*` first, then re-sync to `.agents/skills/*`.
